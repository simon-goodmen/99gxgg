# 共享建材平台 (99gxgg)

## 当前版本
**Version: v1.1.1**

## 版本说明
1. **页面优化**：在网页版“我的”页面添加了版本号显示。
2. **结构重组**：取消了 `web-prototype/server` 目录，将管理后台 `admin` 文件夹移动到 `web-prototype` 根目录下。
3. **安全更新**：移除了包含敏感信息的 `project.config.json` 文件。
4. **统一跳转**：后台管理跳转路径统一更新为 `/admin/dashboard.html`。

## 最近更新记录 (2026-04-01)
- 修正了 `web-prototype` 下的目录层级。
- 实装了登录界面的版本号提示逻辑。
- 清理了 GitHub 的安全扫描警报。
